# Julia's Personal Wesbite

Hi! This is the repo hosting my personal website, using Bioinformatics.ca's Jupyter Book template, which was developed using Jupyter Book.

## Dev Reminders
- Build locally in `PersonalWesbite` with `jupyter-book build --all .`
- Deploy using `ghp-import -n -p -f _build/html`

